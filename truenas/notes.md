## notes

- I installed the image without using docker to a seperate device. The compose file here is for caddy, so that if i need to access remotely I can redirect the port from another device through my main server. You could also use tailscale to avoid this entirely.. this is just a work around to avoid dealing with tailscale on the nas.
