services:
  caddy:
    image: caddy:latest
    container_name: nas-proxy
    restart: unless-stopped
    network_mode: "host"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy_data:/data
      - ./caddy_config:/config
