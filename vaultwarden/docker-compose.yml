services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vw
    restart: always
    environment:
      DOMAIN: "https://vw.cbaier.tech"
      ADMIN_TOKEN: ${VAULTWARDEN_ADMIN_TOKEN}
    volumes:
      - /mnt/vaultwarden/:/data/
    ports:
      - 127.0.0.1:9328:80

